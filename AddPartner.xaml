<Window x:Class="WpfApp1.AddPartner"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp1"
        mc:Ignorable="d"
        Title="AddPartner" Height="auto" Width="auto">
    <Window.Resources>
        <ResourceDictionary>
            <Style TargetType="TextBox">
                <Setter Property="Width" Value="199"/>
                <Setter Property="Margin" Value="5"/>
            </Style>
            <Style TargetType="TextBlock">

                <Setter Property="Margin" Value="5"/>
            </Style>
        </ResourceDictionary>

        
    </Window.Resources>
    <Grid Background="#F4E8D3">
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock Text="Название"/>
            <TextBox Text="{Binding Path=Nazvanie}"/>
            <TextBlock Text="Адрес"/>
            <TextBox Text="{Binding Path=Address}"/>
            <TextBlock Text="ИНН"/>
            <TextBox Text="{Binding Path=INN}"/>
            <TextBlock Text="E-mail"/>
            <TextBox Text="{Binding Path=E_mail}"/>
            <TextBlock Text="Телефон"/>
            <TextBox Text="{Binding Path=Telephone}"/>
            <TextBlock Text="Директор "/>
            <TextBox Text="{Binding Path=Director}"/>
            <TextBlock Text="Рейтинг"/>
            <TextBox Text="{Binding Path=Rayting}"/>
            <ComboBox Name="cbTypes" SelectionChanged="cbTypes_SelectionChanged"  DisplayMemberPath="Nazvanie" SelectedValuePath="ID_Type"/>
            <Button Content="Сохранить" Name="Add" Click="Add_Click"/>
        </StackPanel>
    </Grid>
</Window>
